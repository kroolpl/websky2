---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { Image } from 'astro:assets';

// Import team images
import karolImage from '../assets/images/team/karol.jpeg';
import piotrImage from '../assets/images/team/piotr.jpeg';

const title = "O Nas | Poznaj Zespół WebDesign UK";
const description = "Jesteśmy zespołem polskich ekspertów w UK, którzy tworzą nowoczesne strony internetowe dla firm. Poznaj nas i dowiedz się, jak możemy pomóc Twojej firmie.";

const team = [
  {
    name: "Karol",
    role: "Lead Developer & SEO Specialist",
    image: karolImage,
    title: "Ten od tajemniczego kodu i algorytmów Google",
    quote: "W dzieciństwie rozkręcałem piloty, dziś rozkręcam algorytmy Google'a i biznesowe problemy.",
    description: "Były programista korporacyjny, który uciekł z open-space'u, żeby robić to, co kocha: budować systemy, które NAPRAWDĘ działają i są widoczne w sieci. To on stoi za bezpieczeństwem Twoich płatności online, wydajnością strony i jej pozycją w Google. Wie, jak sprawić, by Twoja firma była widoczna dla klientów w Manchesterze, Birmingham czy Edynburgu. Po godzinach… cóż, wciąż rozkręca piloty. Tym razem jednak składa je z powrotem."
  },
  {
    name: "Piotr",
    role: "UI/UX Designer",
    image: piotrImage,
    title: "Ten, co ma oko do piękna",
    quote: "Dla mnie projekt to jak opowieść – ma wciągać od pierwszej sekundy.",
    description: "Fotograf-amator, który zamiast zdjęć, zaczął kadrować piksele. To on sprawi, że Twoja strona będzie wyglądać jak strona TOP brytyjskiej firmy, ale z polskim sznytem. Wie, że \"profesjonalnie\" nie musi znaczyć \"sztywno\". Jego ulubione słowo? User-friendly – zarówno w designie, jak i w kontaktach z klientami."
  }
];

const workProcess = [
  {
    icon: `<svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto text-primary-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-6 9l2 2 4-4" />
    </svg>`,
    title: "Słuchamy, zanim zaczniemy kodować",
    description: "Najpierw spotkanie (online lub przy herbacie w Twoim biurze), żeby zrozumieć Twój cel."
  },
  {
    icon: `<svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto text-primary-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
    </svg>`,
    title: "Mówimy po ludzku",
    description: "Zero technicznego żargonu, za to mnóstwo przykładów."
  },
  {
    icon: `<svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 mx-auto text-primary-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
    </svg>`,
    title: "Testujemy na prawdziwych Brytyjczykach",
    description: "Zanim strona wystartuje, pokazujemy ją znajomym z UK. Bo kolorowa czcionka, która podoba się w Warszawie, w Liverpoolu może wywołać… mieszane uczucia."
  }
];
---

<Layout title={title} description={description}>
  <Header slot="header" />

  <!-- Hero Section with animated background -->
  <section class="pt-32 lg:pt-40 pb-20 lg:pb-32 relative overflow-hidden">
    <div class="absolute inset-0">
      <div class="absolute inset-0 bg-gradient-to-br from-blue-50/90 via-indigo-50/80 to-purple-50/90"></div>
      <div class="absolute inset-0 bg-[linear-gradient(to_right,#80808012_1px,transparent_1px),linear-gradient(to_bottom,#80808012_1px,transparent_1px)] bg-[size:24px_24px] animate-grid-fade"></div>
      <div class="absolute top-20 left-10 w-72 h-72 bg-blue-200 rounded-full mix-blend-multiply filter blur-2xl opacity-30 animate-float-slow"></div>
      <div class="absolute bottom-20 right-10 w-72 h-72 bg-purple-200 rounded-full mix-blend-multiply filter blur-2xl opacity-30 animate-float-slower"></div>
    </div>

    <div class="container mx-auto px-4 relative">
      <div class="text-left">
        <span class="inline-block px-6 py-2 rounded-full bg-primary-100/80 text-primary-700 text-sm font-medium mb-6 animate-fade-down backdrop-blur-sm">
          Poznaj Nasz Zespół
        </span>
        <h1 class="text-5xl md:text-6xl lg:text-7xl font-bold text-gray-800 mb-8 leading-tight tracking-tight">
          Nie jesteśmy <span class="text-transparent bg-clip-text bg-gradient-to-r from-primary-600 to-secondary-600">zwykłymi</span> programistami
        </h1>
        <p class="text-xl md:text-2xl text-gray-600 mb-12 animate-fade-up max-w-3xl leading-relaxed">
          Jesteśmy Twoimi partnerami w zdobywaniu brytyjskiego rynku
        </p>
        <div class="glass p-8 rounded-2xl max-w-3xl">
          <p class="text-lg text-gray-700 leading-relaxed">
            Cześć! Jesteśmy Karol i Piotr – dwóch facetów z Polski, którzy po latach życia w UK połączyli siły, żeby pomagać firmom takim jak Twoja. Sami wiemy, jak to jest zaczynać w nowym kraju: szukać klientów, budować zaufanie, tłumaczyć się z obco brzmiącego adresu. Dlatego nie tworzymy "tylko stron". Tworzymy mosty między polską jakością a brytyjskimi możliwościami.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Team Section -->
  <section class="py-20 bg-gray-50/50">
    <div class="container mx-auto px-4">
      <div class="grid md:grid-cols-2 gap-8 max-w-5xl mx-auto">
        {team.map((member) => (
          <div class="glass p-8 rounded-2xl hover:scale-105 transition-all duration-300">
            <div class="relative mb-6 group">
              <div class="absolute -inset-1 bg-gradient-to-r from-primary-600 to-secondary-600 rounded-2xl blur opacity-25 group-hover:opacity-50 transition duration-1000"></div>
              <Image 
                src={member.image} 
                alt={member.name}
                class="relative rounded-2xl w-full h-80 object-cover"
                width={400}
                height={500}
                quality="max"
              />
            </div>
            <h3 class="text-2xl font-bold mb-2">{member.name}</h3>
            <p class="text-primary-600 font-medium mb-4">{member.title}</p>
            <blockquote class="text-gray-600 italic mb-6">
              "{member.quote}"
            </blockquote>
            <p class="text-gray-700 leading-relaxed">
              {member.description}
            </p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Why Us Section -->
  <section class="py-20">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold mb-8 gradient-text">
          Dlaczego to robimy?
        </h2>
        <div class="glass p-8 rounded-2xl">
          <p class="text-lg text-gray-700 leading-relaxed">
            Bo pamiętamy nasze pierwsze zlecenie w UK – firmę cateringową, której strona wyglądała jak menu z 2005 roku. Po naszej zmianie klient dostał 3 nowe kontrakty w miesiąc. Wtedy zrozumieliśmy: dobra strona to nie wydatek. To najlepszy handlowiec, który nigdy nie idzie na L4.
          </p>
        </div>
      </div>

      <h3 class="text-2xl font-bold text-center mb-12">Jak pracujemy?</h3>
      <div class="grid md:grid-cols-3 gap-8">
        {workProcess.map((step) => (
          <div class="glass p-8 rounded-2xl text-center hover:scale-105 transition-all duration-300">
            <div class="mb-6" set:html={step.icon} />
            <h4 class="text-xl font-semibold mb-4">{step.title}</h4>
            <p class="text-gray-600">{step.description}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20 bg-gradient-to-br from-primary-50 to-secondary-50">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <h2 class="text-3xl md:text-4xl font-bold mb-8 gradient-text">
          Gotowy na zmianę?
        </h2>
        <p class="text-xl text-gray-600 mb-12">
          Napisz: "Hej, chcę porozmawiać" – umówimy się na wirtualną kawę i bez zobowiązań pokażemy, jak może wyglądać Twoja nowa strona.
        </p>
        <a 
          href="/kontakt" 
          class="inline-flex items-center px-8 py-4 rounded-xl bg-gradient-to-r from-primary-600 to-secondary-600 text-white font-medium hover:scale-105 transition-all duration-300 shadow-lg shadow-primary-500/20"
        >
          Porozmawiajmy
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
          </svg>
        </a>
      </div>
    </div>
  </section>

  <Footer slot="footer" />
</Layout>

<style>
  /* Reuse existing styles from index.astro */
  .gradient-text {
    background: linear-gradient(135deg, #2563eb 0%, #4f46e5 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .glass {
    background: rgba(255, 255, 255, 0.8);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  /* Add new animation styles */
  @keyframes blob {
    0% {
      transform: translate(0px, 0px) scale(1);
    }
    33% {
      transform: translate(30px, -50px) scale(1.1);
    }
    66% {
      transform: translate(-20px, 20px) scale(0.9);
    }
    100% {
      transform: translate(0px, 0px) scale(1);
    }
  }

  .animate-blob {
    animation: blob 7s infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animation-delay-4000 {
    animation-delay: 4s;
  }

  @keyframes float-slow {
    0%, 100% { transform: translate(0, 0) rotate(0deg); }
    50% { transform: translate(10px, 15px) rotate(2deg); }
  }

  @keyframes float-slower {
    0%, 100% { transform: translate(0, 0) rotate(0deg); }
    50% { transform: translate(-15px, -10px) rotate(-2deg); }
  }

  @keyframes grid-fade {
    0%, 100% { opacity: 0.5; }
    50% { opacity: 0.3; }
  }

  .animate-float-slow {
    animation: float-slow 8s ease-in-out infinite;
  }

  .animate-float-slower {
    animation: float-slower 12s ease-in-out infinite;
  }

  .animate-grid-fade {
    animation: grid-fade 4s ease-in-out infinite;
  }
</style> 